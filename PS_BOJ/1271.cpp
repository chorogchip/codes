#include<iostream>
#include<cstdlib>
#include<algorithm>
#include<vector>
#include<cstring>
#include<string>
#define fore(i, n) for (int i = 0; i != (n); ++i)
#define FAST cin.sync_with_stdio(false); cin.tie(nullptr);
#define endl '\n'
using namespace std;

char c;
int a[1010], b[1010], res[1010], s1, s2, g;

int main() { FAST
    int i = 0;
    while ((c = getchar
    int s1 = N.size(), s2 = M.size();
    int g = s1 - s2;
    for (int i = g; i >= 0; ++i) [
        for (int j = s2; j >= 0; --j) if (N[])



    ]
}
